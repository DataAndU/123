<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Debug builds only: allow plain http:// so the server address can point at
         a local/dev backend (e.g. 10.0.2.2) without a TLS certificate. -->
    <application android:usesCleartextTraffic="true" tools:ignore="GoogleAppIndexingWarning"
        xmlns:tools="http://schemas.android.com/tools" />
</manifest>
