<?xml version="1.0" encoding="utf-8"?>
<!--
  Belt-and-suspenders privacy rule: even though android:allowBackup is
  false at the application level, this explicitly excludes the encrypted
  database and datastore from any future device-transfer or cloud backup
  path so personal data never leaves the device.
-->
<data-extraction-rules>
    <cloud-backup disableIfNoEncryptionCapabilities="true">
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
